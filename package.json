{
  "name": "english-for-seasonal-workers",
  "version": "0.1.0",
  "description": "Free offline RU-only A0-A1 English course for CIS seasonal farm workers in the UK",
  "private": true,
  "scripts": {
    "audit": "node scripts/audit.js && node scripts/dedup.js && node scripts/snowball.js && node scripts/reader-audit.js && node scripts/check-bre.js && node scripts/check-transcr-safe.js && node scripts/check-transcr-consistency.js",
    "verify-ipa": "node scripts/verify-ipa.js",
    "verify-transcr": "node scripts/verify-transcr.js",
    "serve": "node scripts/serve.js"
  },
  "license": "MIT"
}
