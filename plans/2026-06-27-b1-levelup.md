# B1 «level-up» — пост-урочные пакеты (план, 2026-06-27)

**Статус:** план, грилл закрыт. **Сборка НЕ начата** (токен-гигиена — каждый пакет за свежий чат).
**Повод:** курс **B1 (L24–L36) ПОЛОН и запушен** (HEAD `b5fc9bf`, `CACHE_VERSION = esw-v95`,
весь B1 на `origin/master`, GitHub Pages передеплоен). Это «Открытые вопросы #7» плана
`plans/2026-06-26-b1-scope.md` — четыре пакета ПОВЕРХ уроков, как закрывали A2 «level-up»
(`plans/2026-06-24-a2-levelup.md` Фазы 1–3 + `plans/2026-06-24-a2-phase2-scenarios.md`).

**Решено владельцем (2026-06-27, грилл этого плана):**
1. **Ридер B1 — ОБА:** 13 пер-урочных глав 24–36 в основной «Истории Ахмада» **+** финальная книга-веха.
2. **Сценарии B1 — все 4:** звонок по телефону · рассказ о травме у врача · вежливая жалоба/эскалация · передать сообщение коллеги.
3. **Разговорник B1 — 4 новые категории:** На телефоне · Жалоба и проблема · Светская беседа · У врача (запись и приём).
4. **Сертификат B1 + веха «Уровень 3» — зеркало A1/A2, активный** (честная отметка без фейкового №; заморозка «до живого A0-теста» неактуальна — тест снят 2026-06-25, `[[live-a0-test-removed]]`).

> ⚠️ Перед стартом ЛЮБОГО пакета прочитать: `CLAUDE.md` (контракт, золотой стандарт, грабли), `DESIGN.md`,
> хвост `STATE.md` (что готово), `plans/2026-06-26-b1-scope.md` (метрика B1, движок), память:
> `naturalness-check-is-mandatory-gate`, `lesson-content-naturalness-anchor`, `bre-praise-good-job-vs-well-done`,
> `contractions-break-snowball-tokenizer`, `b1-past-narrative-snowball-traps`, `b1-past-perfect-needs-anchor`,
> `snowball-whitelist-not-stemmed-inflections`, `b1-common-words-not-in-known-set`, `orphan-lexicon-entries-must-be-live`,
> `reader-book-in-progress`, `certificate-honest-completion-record`, `workflow-fanout-session-limit`.

---

## Инвариант (риск №1) — общий для всех пакетов

«Вау/польза» обязана служить **ЦА** (нулевой английский, низкая цифровая грамотность, бюджетный Android,
offline), а не впечатлять владельца. **Владелец ≠ репрезентативный пользователь** и **с нулевым английским
не приёмщик натуральности**. Технические инварианты: vanilla JS, offline-first, zero-budget, без фреймворков,
без тёмной темы, эмодзи ≤Unicode 11, визуальный «вау» = только лёгкий CSS/инлайн-SVG.

**Натуральностный гейт НЕ снят** для всех полей с продакшн-английским (ридер/сценарии/разговорник):
корректность сверяется с 5 эталонами + `b1_digest.md`, а не со слухом владельца. Зоны повышенной
ответственности (деньги/NHS/визы) — **нейтрально**, без юр./мед./визовых/earnings-claims: «спрашиваю /
сообщаю / прошу», 999/111 только в RU-обёртке. Сертификат — RU + уже-проверенный англ. шаблон → лёгкий гейт.

---

## Переиспользуемые движки (факты из кода — НЕ строить заново)

| Движок | Файл | Что уже есть | Аудит-привязка |
|---|---|---|---|
| **Ридер (мультикнига)** | `app/reader.js` `window.BOOKS` | Полка из 5 книг; 2 режима гейта: **пер-главный снежок** (`ahmad`, глава N разблок за урок N, лексика 1..N) и **fixed-level** (madina/bobur/survival/ahmad3: `gate`=разблок целиком, `level`=кап лексики). `glossary[]` ≤5 синих слов/глава. | `scripts/reader-audit.js`: `cap = book.level ?? chapter.id`, `gate = book.gate ?? chapter.id`. `activeWhitelist(cap)` = **все `PARTICIPLES` + кумулят. `WHITELIST[1..cap]` + `NAMES`** → narrative-тенсы B1 «просто работают». Ловит мёртвый глосс (`reader-audit.js:73`), gate-несуществующий-урок (`:91`). |
| **Сценарии (роль-плей)** | `app/scenarios.js` `window.SCENARIOS` | A2-движок готов: `renderScenarioList`/`renderScenario` + роутинг `#scenario/<id>` + хаб-плитка «🆘 Ситуации». Граф узлов, спикеры `m/w/c/d/a/n`, исходы good/ok/bad. | `scripts/scenario-audit.js`: **авто-масштаб** на `maxId` (=36) → union всех `words[]` 1..36 + `activeWhitelist(36)` + NAMES + числа. **Код менять НЕ нужно.** Структура графа + transcr/tr_ru на каждой строке. |
| **Разговорник** | `app/phrasebook.js` `window.PHRASEBOOK` | 9 категорий (understand/work/shop/health/documents/sos/banking/wages/agency). `renderPhrasebook` итерирует категории. **Вне снежка/аудита-лексики.** | НЕ в vocab-аудите, НО `check-transcr-consistency` покрывает разговорник (знакомое ≥5 букв = единая transcr) + `check-bre`. |
| **Сертификат/веха** | `app/app.js` | **B1-скаффолдинг УЖЕ есть:** `level_b1`/`journey_l3` строки (`:89,:94`), `hasB1`/`b1Total`/`b1Done`/`b1Complete` (`:160-163`), хаб-секция B1 с локом (`:310-312`), путь-шаг 3 (`:374`). `A2_MAX=23` фикс, B1 декуплен от `LESSONS.length` (`:149-154`). | — (RU + проверенный шаблон) |

---

## Пакет D — Сертификат B1 + веха «Уровень 3» (СТРОИТЬ ПЕРВЫМ) — ✅ ПОСТРОЕН 2026-06-27

> **Готово (`app/app.js` + `app/style.css`, `CACHE_VERSION` v95→v96).** `renderCertificate(level)` и `renderMilestone(level)`
> генерализованы на 3 уровня (a1/a2/b1); тройные кросс-линки серта (A1↔A2↔B1, гейт по пути); триггер вехи B1 в `finishMastered`;
> store `certDateB1`/`msB1Shown`; UI `cert_b1_*`/`ms_b1_*`/`cert_hub_sub_three`; печать-SVG + заголовок вехи бирюзовые. Гейт пройден
> (audit 8/8, превью mobile: серт печатается / веха срабатывает 13/13 / путь 3 уровня / кросс-линки / locked-состояние / 0 ошибок).
> Детали — `STATE.md` (запись 2026-06-27). ✅ **Запушено** (`b5fc9bf..f4fcc5b` → `origin/master`, Pages передеплоен). **Следующий — Пакет C (разговорник B1).**

**Почему первым:** самый дешёвый, самый видимый payoff («Уровень 3 закрыт» сразу при открытии), audience-safe,
без натуральностного гейта (RU + уже-сверенный англ. шаблон серта). Зеркало Фазы 1 A2-level-up.

**Объём (что УЖЕ есть — НЕ трогать):** хаб-секция «Уровень 3 · B1» (аккордеон + лок), путь A0→A1→A2→B1
(шаг 3), `b1Complete()`/`b1Done()`/`b1Total()`. Эти части рендерились по мере сборки L24–L36 (превью-отчёты
STATE «хаб Уровень 3 · B1 = N/13»).

**Что ДОСТРОИТЬ (`app/app.js`):**
1. **Генерализовать `renderCertificate(level)`** с бинарного `a2`-флага (`:844 var a2 = level === "a2"`) на 3-уровневый
   (a1/a2/**b1**): `total=b1Total()`, `done=b1Done()`, `dateKey="certDateB1"`, печать `B1`, `levelLine =
   "Pre-intermediate self-study course · CEFR B1 level"`, `courseLine = "has completed the pre-intermediate self-study course"`.
   Чище — заменить `var a2 = …` на `var lvl = level` и развести 3 ветки (a1=undefined).
2. **Генерализовать `renderMilestone()`** (`:917`, сейчас хардкод A2-строк) на параметр уровня ИЛИ b1-вариант:
   `ms_b1_title`/`ms_b1_sub`/`ms_b1_cert`.
3. **Триггер вехи** (`:1325`, рядом с A2-веткой): `if (les.id > A2_MAX && b1Complete() && !store.msB1Shown) { store.msB1Shown = true; save(store); renderMilestone("b1"); return; }`.
4. **store-поля:** `store.certDateB1` (как `certDateA2`, `:192`), `store.msB1Shown` (как `msA2Shown`, `:193`).
5. **UI-строки `UI.ru`:** `cert_b1_title`, `cert_b1_sub`, `cert_b1_locked`, `cert_see_b1`, `ms_b1_title`, `ms_b1_sub`, `ms_b1_cert`.
6. **Кросс-линки серта:** A2↔B1 (как A1↔A2, `:854-861`) — тройная навигация между «Мой прогресс», A2, B1.

**Честная формулировка (инвариант `[[certificate-honest-completion-record]]`):** тот же двуязычный дисклеймер
«self-study completion · не официальная квалификация/экзамен/подтверждение уровня», **без фейкового «Certificate No.»**.
Заморозка «до живого A0-теста» **неактуальна** (тест снят) → серт **активный при 13/13** (зеркало A1/A2, которые
уже выдаются). Веха-экран «🎉 Уровень 3 пройден» (лёгкий CSS-`milestone`, без тяжёлых анимаций).

**Зоны риска:** нет (RU + проверенный англ. шаблон, нейтральный).
**Снежок:** не применим (сертификат не графится).
**Гейт:** `npm run audit` зелёный · превью на узком вьюпорте (`done[1..36]` → веха срабатывает 13/13, серт B1
печатается, путь показывает 3 уровня, кросс-линки) · `CACHE_VERSION`++ · `STATE.md` · `DESIGN.md` (если новые классы).

---

## Пакет C — Разговорник B1: 4 новые категории (СТРОИТЬ ВТОРЫМ)

**Почему вторым:** малый, вне снежка-лексики, быстрый; чистые data-добавления в `PHRASEBOOK`.

**Объём:** 4 категории × ~7–10 фраз, поверх 9 имеющихся (всего станет 13). Каждая фраза `{en, transcr, ru}`
(фиксированные чанки, НЕ графятся снежком). Движок (`renderPhrasebook`) уже итерирует категории — **код менять не нужно**.

| Категория | `cat`/icon | Содержание | Якорь натуральности |
|---|---|---|---|
| **На телефоне** | `phone` 📞 | «Can I speak to…», «I'd like to book an appointment», «Can you call me back?», «I'll call again later», «Sorry, wrong number», «Could you spell that?» | CEFR-B1 «on the phone»; Headway Everyday English «on the phone»; EF Practical English; Speakout phone-функция |
| **Жалоба и проблема** | `complaint` ⚠️ | «There's a problem with…», «It's not working», «I'd like to complain», «This isn't right», «Can you fix it, please?», «I've already told the manager» | B1 «making a complaint»; ACAS/gov.uk (нейтрально); EF/Speakout complaint-функции |
| **Светская беседа** | `smalltalk` 💬 | «How's it going?», «Not bad, thanks», «So do I / Neither do I», «That's good news», «Take care», «See you tomorrow» | B1 «making conversation / agreeing & disagreeing / good-bad news»; Headway/EF everyday |
| **У врача: запись и приём** | `appointment` 🩺 | «I'd like to make an appointment», «How long have you had it?» (RU-объяснение), «It started a week ago», «I've had this pain for…», «Can I have a sick note?» | NHS (nhs.uk) — нейтрально; Headway «at the doctor's»; b1_digest doctor-юнит |

**Зоны риска (нейтрально):** `appointment` — NHS-зона: симптомы/длительность, **без диагнозов и мед.-советов** от приложения,
999/111 только в RU-пояснении (как существующая `health`). `complaint` — деэскалация, **без «вам должны / это незаконно»**.
`phone` — нейтрально.

**Снежок:** не применим (разговорник вне vocab-аудита). **НО** обязаны: `check-transcr-consistency` (знакомые ≥5 букв
слова — transcr как в каноне `words[]`; кейс L7 `tomorrow`) + `check-bre` (никаких «Good job», американизмов). Transcr
писать вручную по конвенциям курса (`/w/`→«у», `æ`→«э», non-rhotic).
**Гейт:** разговорник = зона повышенной ответственности (см. шапку `phrasebook.js`) → адверсариальный safety-проход
+ source-cited натуральность (как A2-категории banking/wages/agency). `npm run audit` зелёный · `CACHE_VERSION`++ · `STATE.md`.

---

## Пакет A — Ридер B1: 13 пер-урочных глав 24–36 + финальная книга-веха (СТРОИТЬ ТРЕТЬИМ)

**Объём (честно — самый крупный пакет): ~19 глав.** Narrative-тенсы B1 (Past Continuous/Past Perfect/reported speech) —
**родная стихия** running-story; глоссарий даёт лексический простор. Каждая глава: ~14–17 предложений `{en, ru}` +
`glossary[]` ≤5 (одно слово, синее, тап→перевод+🔊) + 2–3 RU-quiz `{q_ru, opts_ru[4], c}`.

### A.1 — 13 пер-урочных глав 24–36 (в основную «Историю Ахмада», массив `READER`)
Глава N разблок за `store.done[N]`, лексика **только 1..N** (строже!). Грамм-дуга по уроку:

| Гл. | Грамм-фокус нарратива (= урок) | Сюжетный момент 4-го сезона |
|---|---|---|
| 24 | Past Continuous + контраст (when/while) | смена идёт, прерывание |
| 25 | **Past Perfect** (had+V3) + sequencing | вспоминает, что было ДО приезда |
| 26 | **Reported speech** (said/told) | пересказ слов Тома/агентства |
| 27 | PP for/since + How long? | сколько уже на ферме / болит спина |
| 28 | Present Perfect Continuous | что делал весь день |
| 29 | will/won't + going-to | планы на сезон |
| 30 | 1st conditional + may/might | «если дождь — …» |
| 31 | have to/must/should (rules) | правила безопасности на складе |
| 32 | Passive + made of | как пакуют/из чего |
| 33 | 2nd conditional (If I were…) | мечты/гипотетика |
| 34 | used to + relative clauses | каким был раньше / описание людей |
| 35 | verb patterns + phrasal verbs | бытовые действия смены |
| 36 | as…as / quantity / articles | подведение итога сезона |

### A.2 — Финальная книга-веха (standalone, как `ahmad3`)
`gate=36, level=36` → разблок целиком за L36, **вся лексика 1..36 + причастия доступны с 1-й главы** (снежок-лёгкая).
~6 глав, мирит с тем, как закрыли A2 (Ahmad-3 «третий сезон»). **Тема/герой — минорная развилка (см. «Открытые»):**
рекомендация — **капстоун-ретроспектива** (Ахмад наставляет нового рабочего ИЛИ оглядывается на весь путь A0→B1),
чтобы читалась как итог, а не дубль пер-урочных глав. Цель — показать диапазон B1-нарратива (Past Perfect + reported
speech + conditionals в связном тексте).

**Источники натуральности:** `Books B1/_render/b1_digest.md` (фермер-адаптация) + `headway_full.txt`/`ef_full.txt`
(грепать юнит: Past Cont — Headway U3/EF F2; Past Perfect/reported — Headway U14/EF F9; PP for-since — Headway U7/EF F7)
+ Speakout PNG `so_0NN.png` по нужде. Narrative-тенсы — `lesson-content-naturalness-anchor`.

**Зоны риска (нейтрально):** гл. 26 reported speech про зарплату/смены — «He said that…» нейтрально, без юр./визовых
claims. Гл. 27 (спина) — NHS-зона, без мед.-советов. Авария/деньги — осведомлённость, не инструкция.

**🔴 Снежок-ограничения ридера (те же `allowed()`, что в уроках — критично для пер-урочных глав):**
- **`b1-past-narrative-snowball-traps`:** удвоенные регулярные прош. (stopped→stopp) и **иррегулярные simple-past НЕ из
  `PARTICIPLES`** (came/rang/saw/went/knew/took/gave/spoke) падают в снежке → брать формы, которые ОДНОВРЕМЕННО
  причастия (won/found/paid/made/said/left/sold/told), либо перефразировать. Повторяется в КАЖДОЙ прош.-нарративной главе.
- **`b1-past-perfect-needs-anchor`:** каждый had+V3 пара́ить с поздним Past Simple-якорем (аудит-слепо, ловят ревьюеры).
- **`snowball-whitelist-not-stemmed-inflections`:** инфлексии whitelist/PARTICIPLES-базы (comes/came/goes) проваливаются —
  база/words[]-глагол/точная whitelist-форма.
- **`b1-common-words-not-in-known-set`:** big/small/man/people/place/live/know — **node-пробить ДО записи** (часть только в
  ридер-глоссах, не в `words[]`). `place`/`live` вошли в `words[]` L34.
- **Глосс — одно слово** (`reader-audit:53` whitespace=ошибка; «pick up» нельзя) + **обязан быть живым** (`:73-74` мёртвый
  глосс=ошибка, `[[orphan-lexicon-entries-must-be-live]]`). ≤3-букв плюрал стеммится в кашу — избегать в проверяемых полях.
- Причастия для passive/perfect/reported — **уже в центр. `PARTICIPLES`** (`lib.js:273`) → had been/written/paid «просто работают».

**Движок:** только data — добавить главы 24–36 в `READER`; добавить финальную книгу в `BOOKS` (`gate/level=36`). reader-audit
авто-обрабатывает (`cap=level??id`). Превью: книга разблокируется, главы читаются, глоссы синие+попап, quiz считается.
**Каденс:** пер-урочные главы батчами по грамм-дуге (несколько за чат, ридер снежок-легче урока), финальная книга — отдельно.
Фан-аут модест (`[[workflow-fanout-session-limit]]`).

---

## Пакет B — Сценарии выживания B1: 4 новых (СТРОИТЬ ПОСЛЕДНИМ)

**Почему последним:** строжайшая safety (NHS/жалоба), полный 4-слойный гейт + source-cited safety per-сценарий.
A2 уже закрыл `pay/agency/accident/nhs/camp` — **B1 НЕ дублирует**, берёт НОВЫЕ ситуации + B1-грамматику.

| ID | Ситуация | B1-грамматика | Safety-зона |
|---|---|---|---|
| `phone` 📞 | Звонок: записаться к врачу / в агентство, перенести смену | «on the phone»-функция (Can I speak to / I'd like to book / I'll call back) | нейтрально |
| `injury` 🤕 | Рассказ о травме у врача (глубже A2-NHS) | **Past Continuous** «I was lifting when…» + **PP for/since** «I've had this pain for…» | 🔴 NHS — 999/111 в RU, без диагнозов |
| `complaint` ⚠️ | Проблему не решили, вежливо настоять/эскалировать | **reported speech** «I told them that…» + should/have to | 🔴 ACAS/gov.uk нейтрально, деэскалация |
| `relay` 📨 | Передать/получить слова менеджера или коллеги | **reported speech** (he said / she asked) | нейтрально (без pay/visa claims) |

**Источники натуральности:** 5 эталонов + `b1_digest.md` + предметные: **nhs.uk** (injury — когда 999/111), **ACAS/gov.uk**
(complaint), «on the phone» Headway Everyday/EF Practical/Speakout (phone), reported speech Headway U14/EF F9 (relay/complaint).

**🔴 Снежок-ограничения сценариев:** scenario-audit = union всех `words[]` 1..36 + `activeWhitelist(36)` (вкл. `PARTICIPLES`)
+ NAMES + числа. **Новые слова вводить НЕЛЬЗЯ** (режим применения, не обучения — нет глоссария, в отличие от ридера).
B1-грамматика работает: reported said/told (PARTICIPLES) + asked→ask (WL13); Past Cont was+ing; PP for/since (for/since=WL27);
have to/must/should (WL). Контракции haven't/I've сворачиваются токенайзером (`[[contractions-break-snowball-tokenizer]]`).
**node-пробить каждый токен ДО записи.** Спикеры — reuse `m/w/c/d/a/n`; если нужен «регистратор» — добавить метку `r`
(движок просто тегирует, голос один en-GB).

**Зоны риска (строжайше):** `injury` — NHS: симптомы/нарратив, **без диагнозов/первой помощи**, 999/111 в RU `outcome_ru`.
`complaint` — нейтральная настойчивость, **без «вам должны / это незаконно»**, ACAS-grounded. `relay` — «He said that…»
нейтрально, без pay/visa/legal claims. `phone` — нейтрально.
**Гейт (зона повышенной ответственности):** (1) `npm run audit` вкл. `scenario-audit` зелёный; (2) чек-лист; (3)
адверсариальные ревьюеры язык/IPA + педагогика/**safety**; (4) **source-cited safety + натуральность** `/lesson-naturalness-check`
(метод) против 5 эталонов + предметных. Строить `injury` (NHS) последним, отдельным проходом.

---

## Порядок и каденс (каждый пакет — свой свежий чат)

1. **D — Сертификат/веха** (дёшево, видимо, audience-safe, без натуральностного гейта).
2. **C — Разговорник** (мал, вне снежка-лексики, быстро).
3. **A — Ридер** (крупный; пер-урочные главы 24–36 батчами по дуге, затем финальная книга-веха).
4. **B — Сценарии** (строжайшая safety, полный 4-слойный гейт; `injury` — последним).

**Каждый пакет:** offline/vanilla/zero-budget; превью на **узком (бюджетном) вьюпорте**, читаемость для A0; бамп
`CACHE_VERSION` (`app/sw.js`); обновить `STATE.md`; коммит **по именам файлов**; **НЕ пушить без явного разрешения
владельца** (push авто-деплоит на живой сайт). Фан-аут агентов — модест (`[[workflow-fanout-session-limit]]`).
Грабля превью (DESIGN.md): скриншот таймаутит на `backdrop-filter` → проверять `preview_eval`/computed-style.

---

## Открытые вопросы (минорные — решить в начале своего пакета, не блокеры плана)

1. **Финальная книга-веха (Пакет A.2): тема/герой.** Рекомендация — Ахмад-капстоун (ретроспектива A0→B1 / наставляет
   новичка). Альтернатива — вернуть Мадину на B1. Решить в чате ридера.
2. **Иконки новых разговорник-категорий** (`complaint` ⚠️ vs 📢; `smalltalk` 💬 vs 🗣️) — честный эмодзи на месте.
3. **Сценарий `phone`: нужен ли спикер-регистратор `r`** или хватает `a`/`d`/`m` — решить при сборке (движок просто тегирует).

---

## Статус грилла: ✅ ЗАКРЫТ (2026-06-27). Решения владельца зафиксированы (максимальный объём по 4 пакетам).
**Прогресс сборки:** ✅ **Пакет D (сертификат/веха) ПОСТРОЕН** (2026-06-27, не запушен). Следующее действие — **Пакет C
(разговорник B1)** отдельной сессией, затем A (ридер) и B (сценарии). Каждый пакет — свежий чат (токен-гигиена).
