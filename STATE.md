# STATE.md — текущее состояние курса

Журнал готовности уроков и отчёты внешних сверок. Читается по требованию (при
создании/правке урока, проверке «что сделано», подготовке следующего урока).
**Обновлять после каждого готового урока** (часть «Процесс работы» в `CLAUDE.md`).
Дорожная карта — `plans/2026-06-18-full-a0a1-scope.md`.

## Текущее состояние (2026-06-20)

**Построено и проверено (аудит зелёный):**
- Движок: хаб + экран урока (табы Grammar/Words/Dialogue/Phrases/Quiz) + Web Speech +
  квиз-тренажёр + streak + PWA.
- **Уроки 1–15 «золотые» (КУРС A0–A1 ПОЛОН 🎉):** у всех есть `everyday` (функц. блок); в `quiz` типы `[LISTEN]`
  (аудирование: ▶ TTS → выбрать перевод, англ. текст скрыт) и `[GIST]` (чтение: RU-вопрос по диалогу).
  L1 — am/is/are + национальности (35 слов); L2 — притяжательные my/your/his/her; L3 — this/these + a/an;
  **L4 — числа/время/дни/деньги £; L5 — места + предлоги (Where is…?); L6 — Present Simple + do/does +
  наречия частоты + объектные me/him/us/them** (закрыта отложенная дыра); **L7 — Present Continuous +
  будущее-план (am/is/are + -ing; «I am starting tomorrow»); 30 новых -ing-глаголов (spray/sweep/read/
  climb/feed…), будущее и учится, и проверяется в quiz**; **L8 — have/has + do/does + одежда/PPE + цвета
  (30 слов); плейн `have/has` БЕЗ `have got`/сокращений (решение грилла — не трогать токенайзер; `have got`
  узнаваемой пометкой); quiz-фокус на согласовании he→has, does+have-база**; **L9 — there is/there are +
  some/any + комната/мебель/техника вагончика (room/bed/shower/heater/kettle/fridge…) + 8 состояний-прилагательных
  (broken/wet/cold/hot/heavy/empty/full/ready через is/are-предикатив); плейн `is not`/`are not`; закрыта дыра
  state-прилагательных, перенесённая из L8.** **L10 — how much/how many + would like (вежливое «хочу») +
  еда/напитки/покупки Tesco (bread/milk/rice/eggs/tin/biscuit/pasta/honey… 30 слов BrE); все ценовые вопросы —
  на неисчисляемых/ед.ч. (much↔many без скрытого price-исключения, чтобы не конфликтовать с ключом quiz);
  плейн `would not like` + «No, thank you»; диалог «магазин» worker↔cashier с сходящимся итогом-корзиной.**
  **L11 — this/that/these/those (близко/далеко × один/много, с is/are) + транспорт/поездка (bus/coach/train/
  taxi/lorry/ticket/station/platform/seat/airport… 30 BrE-слов); диалог «станция» worker↔staff (спросить
  автобус/купить билет), everyday «На станции».** Демонстративы this/that (L1) + these/those (L3) уже в whitelist.
  **L12 — was/were (прошлое от be) + was/were born + даты (12 месяцев/year) + личные данные/документы
  (born/date/birthday/age/address/number/form/email/card/contract/married/single/signature/postcode…); диалог
  «регистрация» manager↔worker (имя/дата рождения/адрес/контракт), everyday «Заполнить форму» (date of birth/
  sign here/National Insurance number). Зона повышенной ответственности — без юр./визовых утверждений.** Снежок-
  whitelist по урокам — в `scripts/lib.js` (`WHITELIST[1..15]`; L10 — `many/would/like/of`, L11 — `which/at`,
  L12 — `was/were/when`, L13 — `did/last`+неправ.формы, L14 — `can/cannot`, L15 — `love/hate/want`).
  **L13 — Past Simple (правильные +-ed / неправильные) +/−/? + отчёт смены. 30 новых слов: 14 глаголов
  (eat/drink/leave/buy/pay/drive + правильные collect/deliver/unload/repair/ask/answer/return/park) + 16
  смена/время (yesterday/ago/report/shift/boss/problem/mistake/accident/late/early/pallet/weekend/lunch/minute/
  hard/overtime); диалог «отчёт менеджеру». Ключевое: отрицание/вопрос = `did`+БАЗА. `WHITELIST[13]=did/last` +
  6 неправильных форм `ate/drank/left/bought/paid/drove` (вводятся уроком; обойдена ловушка стеммера на `-e`-глаголах
  arrive/use → взяты согласные-окончания).**
  **L14 — can / cannot (умение + Can you/Can I просьбы) + здоровье/тело/болезнь + чувства/welfare (hand/arm/leg/
  foot/eye/ear/tooth + sick/ill/pain/hurt/cough/fever/headache/medicine/doctor/nurse/chemist/plaster + tired/hungry/
  thirsty/happy/better/emergency/ambulance); диалог worker↔ВРАЧ (спикер `d` впервые); everyday «У врача и экстренная
  помощь» (I feel sick / Call an ambulance / 999–111). Зона повышенной ответственности NHS: 999/111 сверены с
  gov.uk, без мед. диагнозов/советов приложения. `can't` ломает снежок → плейн `cannot`/`can not` в снежок-полях.**
  **L15 (последний) — want / like / love / hate (+ предмет ИЛИ глагол с -ing) + досуг/соц. 30 новых: досуг
  (music/football/sport/film/game/song/dance/swim/run/cook/party/holiday/hobby/picture/radio/news/story) +
  BrE-соц. (cheers/lovely/mate) + качества (fun/bored/busy/favourite/free/nice/great/relax/enjoy/joke); диалог
  «болтовня в перерыве» (m↔w), everyday «Перерыв и общение». Грамматика: like/love/hate + -ing/предмет, want +
  предмет, отриц./вопрос через do. Ловушки: инфлексии whitelist-глаголов (loves/likes) И небезопасные -ing
  (dancing/running) ломают снежок → только база (I/we love) + безопасные -ing (picking/cooking/cleaning).
  `WHITELIST[15]=love/hate/want` (like уже WL10).**
- **Разговорник** (`app/phrasebook.js`, вход из хаба): 6 категорий / 62 фразы, адверсариально
  проверены (вкл. safety health/SOS/documents).
- **Режим повторения (SRS)** — флешкарты из пройденных слов (хаб 🔁). **Сертификат** 🏆 —
  церемониальный «Certificate of Completion» (англ. документ + RU-обёртка), выдаётся только при
  15/15; до этого экран прогресса с замком. **Честная отметка (B1, 2026-06-22, после совета 5/5 +
  грилла):** убран фейковый «Certificate No.» (был коллизионный хэш имя+дата — маскировался под
  ID реестра), формулировка «self-study completion», двуязычный заметный дисклеймер «не официальная
  квалификация/экзамен/подтверждение уровня». Печать/PDF (`window.print()`), `esw-v51`. **Заморожен**
  до живого A0-теста: offline-стек не даёт верифицируемую корочку, любой может «выпустить» на любое имя.
- Git ~85 коммитов, чистая история; грамматика подтверждена 5 курсами (`coverage-matrix.md`).
  Всего 455 слов в 15 уроках (L1=35, L2–L15 ≈30). **🎉 Все 15 уроков MVP A0–A1 построены и проверены.**
- ⚠️ **L13 построен как осознанный override совета (2026-06-20):** совет (5/5, high) + грилл постановили
  СНАЧАЛА живой A0-тест (тест Б, `plans/2026-06-20-validation-criteria.md`), потом L13. Владелец выбрал
  строить дальше; **живой тест по-прежнему НЕ проведён** — главный непроверенный риск (продукт ни разу не
  встречал реального пользователя ЦА). Пред-тестовые онбординг-фиксы сделаны (англ. жаргон с хаба, счётчик слов).
- **Сверка IPA внешним якорем:** `npm run verify-ipa` тянет Received Pronunciation из Wiktionary
  для всех слов и сверяет с `pn` (404/455 совпали, вкл. L15; остаток — нотация, US-варианты снапшота,
  топонимы или нет в Wiktionary). Разрывает само-референтную LLM-петлю по фонетике (риск №1 совета).
  Вне `npm run audit` (сеть/медленно). L9 чист: флаги только нотация/варианты — `shower` /ˈʃaʊə/ (снапшот
  подтянул омограф «show-er»), `room` /ruːm/ (легитимный RP-вариант наряду с /rʊm/), слоговые `kettle/candle/broken`.
  **L10 чист: 0 расхождений с Wiktionary RP; 6 слов «не найдено» (cheese/butter/honey/tomato/carrot/banana) —
  частотные, BrE-IPA верны (tomato/banana с долгим /ɑː/, не US). L11 чист: 3 флага — нотация/варианты
  (`station` слоговое /ʃn/↔/ʃən/, `garage` /ˈɡærɑːʒ/ — легитимный BrE-вариант наряду с /ˈɡærɪdʒ/, `pavement`
  опц. schwa), приняты. L12 чист: April выровнен к RP /ˈeɪprɪl/; January/February/birthday — нотация, `number`
  /ˈnʌmbə/ верна (Wiktionary-артефакт /ˈnʌmə/).**
- **Сверка transcr звуком:** `npm run verify-transcr` генерирует ожидаемую кириллицу из `pn` по
  конвенции курса и ловит несогласованности крутки (426/455, вкл. L15). Так найдены и исправлены `/w/`→«у»
  (work/worker/welcome) и `van`→«вэн». Вторая половина митигации риска №1 (крутка = фактический ввод A0).
  L9–L13 чисты: новые осознанные флаги — `onion` «аньэн» (/nj/, L10), `queue` «кью» (/kjuː/, L11),
  `January` «джэньюэри» (/nju/, L12), `yesterday` «йестэдэй» (/je/, как `yes`, L13), `ambulance` «эмбьюлэнс»
  (/j/, L14), `music`/`news` «мьюзик»/«ньюз» (/Cj/) и `game` «гейм» (как `gate`, L15); остальные — старые
  исключения L1–L8 (топонимы, three→фри). L13–L15 ipa-флаги (collect/park/mistake/medicine 2-слога/appointment/
  favourite-выровнен/enjoy en-in-вариант) — Wiktionary-артефакты или BrE-варианты, мои BrE верны.
- **Сверка натуральности внешними источниками (`0ab5e1f`):** весь текст уроков + разговорника
  (246 строк: разговорник 62, диалоги 72, grammar-примеры 112) прогнан адверсариальным multi-agent
  workflow против авторитетных ELT/UK-источников (British Council, Cambridge/Oxford, NHS/gov.uk) +
  CEFR A0-A1-канона — каждый вердикт с цитатой-источником, второй проход отклоняет ложные флаги
  (британские `chemist`/`till`/`scales` защищены). Исправлено 2: `scale`→`scales` (BrE мн. ч. для
  весов, L3/L6) и `How much money is it?`→`It is five pounds.` (неидиоматичная коллокация, L4).
  Проверяет естественность ФРАЗ (коллокации/идиоматика/BrE) — ось, которую verify-ipa/verify-transcr
  (только фонетика `words[]`) не видят. Третий внешний якорь против ИИ-петли №1. Вне `npm run audit`
  (сеть/агенты); живой носитель остаётся гейтом перед публичным запуском (B1–B2).
  **Метод кодифицирован в скилл `/lesson-naturalness-check` (`.claude/skills/`, `4dd1f68`) = обязательный
  слой-4 гейт каждого урока; источники с URL — `specs/reference-sources.md`. L9 прогнан им: SHIP, 0 флагов
  (3 ревьюера, `e165775`). L10 прогнан слоями 3+4: SHIP после 5 правок — слой-3 поймал «how much + мн.ч.
  исчисляемых» (скрытое противоречие правилу much/many и ключу quiz) и «Good.» как реакцию на отказ; слой-4 —
  несходящийся итог корзины диалога (£10→£3, деньги = зона повышенной ответственности). L11 прогнан слоями
  3+4 (объединённый workflow, 3 ревьюера + скептик): SHIP после 4 правок — `o'clock` transcr-дрейф «оклок»→«эклок»
  (как L7 tomorrow; L4 ввёл «эклок»), `are`→крутка «а»→«ар» (регрессия от конвенции L1/L3 «Зиз ар», ~14 мест),
  2 эмодзи-обманки (corner 📐→↪️, queue 🧍→👥). Scope сверен с РЕАЛЬНЫМ текстом CEFR A0-A1-PDF: «this/that/these/
  those», «transport & travel», «shopping/ticket» — в каноне (но фразовая сверка по страницам книг ограничена:
  для демонстративов/транспорта готовых рендеров в `_render/` нет, рендера в среде нет → знание ELT + CEFR-текст).
  L12 прогнан слоями 3+4 (safety-акцент, документы): **контент SHIP** — отклонён ложный флаг (12× 🗓️ месяцев =
  честно, как L4 7 дней под 📅). Ревью выявило СИСТЕМНЫЙ баг ассессмента: во всех уроках правильный ответ quiz
  всегда `c:0` (L8–L12 = 12/0/0/0), движок не перемешивает опции → урок проходится тыком первой кнопки, ломая
  streak-гейт для A0. Фикс на уровне движка (shuffle опций + remap `c`), отдельный коммит — чинит все уроки разом.**
  L13 прогнан слоями 3+4 (объединённый workflow): **SHIP после 2 правок** на одной реплике диалога «I asked the
  mechanic» — RU-глосс «позвал»→«спросил» (противоречил карточке `ask=спрашивать`) и transcr `мэканик`→`мэкэник`
  (дрейф от канона L2; правило «transcr во фразах — дословно из words[]»). Грамматика did+база — чисто.
  L14 прогнан слоями 3+4 (усиленный NHS-safety): **SHIP после 1 правки** — эмодзи `chemist` 🏥(больница)→🧴
  (аптека≠больница, обманка в NHS-уроке). Safety чисто: 999/111 точны (gov.uk), `chemist`=валидный BrE,
  «Take this medicine» врача = реплика персонажа (не совет приложения), `can't` только в прозе. Отклонён флаг
  `cough` 😷 (нет эмодзи кашля; маска = ближайший честный, 🤧=чихание ≠ кашель).
  L15 прогнан слоями 3+4: **SHIP после 1 правки** — диалог «I hate work on Sunday» → «I hate **working** on Sunday»
  (голое существительное противоречило правилу урока like/love/hate + -ing и RU-глоссу «работать»; `working`→`work`,
  снежок-чисто). 0 отклонённых. BrE-регистр (cheers/lovely/mate/film/holiday) защищён.
- **Помощь «ещё проще» + YouTube-резерв** (движок `01e9ed1` + ретрофит L2–L7 `7920693`): свёрнутый
  блок в конце grammar-таба для тех, кто не понял правило. Опц. поля `grammar.simple_ru` (`formula` +
  1–2 мини-примера) и `grammar.ytQuery` (ручной RU-запрос). UX: тоггл (closed по умолчанию) → формула +
  примеры с TTS → мелкая последняя-резервная YouTube-ссылка (`target=_blank`, «нужен интернет») +
  инструкция возврата. **Есть на всех построенных уроках L1–L15.** Мини-примеры — дословно из уже
  сверенных таблиц/примеров урока (нет фонетического дрейфа). Аудит/снежок покрывают оба опц. поля.
  Спека: `plans/2026-06-19-grammar-help-youtube.md`.
- **Ретрофит охвата CEFR A0–A1 (2026-06-20, `plans/2026-06-20-cefr-coverage-gap-closure.md`):** сверка
  по 3 источникам (CEFR-PDF канон + реальные страницы Headway юнитов 1–14 в `_render/` + грепы `data.js`)
  показала курс ~95% полным; закрыты узкие дыры **ретрофитом** (решение владельца: строить сейчас · ретрофит ·
  Tier 1+2). **Tier 1:** числа 11–100 (L4 `everyday.html`-grid +teen/+ty + фразы), правило мн.ч. -s/-es
  (L3 grammar-grid + 2 снежок-чистых квиза: формы `crates`/`boxes` стеммятся к `crate`/`box`), алфавит +
  «произнести имя по буквам» (L1 `everyday.html` 26 букв BrE «зэд» + spell-фразы). **Tier 2:** заказ в
  столовой (L15 «Can I have a sandwich, please?»), советы Why don't you…/You should (L14), языки I speak…/
  Do you speak Russian? (L1). **«Семья» НЕ строилась — уже покрыта в L2** (12 слов; стале-`structure.md` врал
  «farm roles» → сверка `data.js` спасла). **Движок:** опц. поле `everyday.html` (HTML-справочник над
  фразами в табе «Фразы», вне снежка/аудита; `app.js` `everydayBody`), `CACHE_VERSION` v31→v32. Числа/алфавит
  — НЕ в `words[]` (everyday вне снежка → 455 слов без изменений). Аудит зелёный; рендер L1/L4 проверен в
  превью (0 ошибок консоли); **слой-4 гейт натуральности (3 source-cited ревьюера + скептик): SHIP, 0 флагов**
  (контент почти дословно из Headway + факты букв/чисел). **Tier 3 (tap-to-build) построен следом** (см. ниже).
- **tap-to-build — продуктивный навык построен (2026-06-20):** новый quiz-тег **`[BUILD]`** — движок
  перемешивает плитки-слова, пользователь собирает фразу тапами, проверка порядка (верный → TTS + expl;
  неверный → красный фидбэк с правильной фразой). По **1 заданию на каждый из 15 уроков** (фраза под
  грамматику урока, снежок-чисто: I am a worker / She packs the box / I was born in May …). Движок
  `app.js` `showBuild` + CSS `.tile`/`.build-line`; `audit.js`/`snowball.js` поддерживают поле `build`
  (вместо `opts/c`). `CACHE_VERSION` v32→v33. Аудит зелёный; оба пути (верный/неверный) проверены в
  превью; **слой-4 source-cited гейт: SHIP** (Cambridge Grammar «would like»/born-in-month + аналоги
  Headway/EF). **Закрывает последнюю CEFR-дыру — продукцию** (writing-postcard/short-description суррогат).
  Скилл-баланс полон: recognition (MCQ) + listening (`[LISTEN]`) + reading (`[GIST]`) + **production (`[BUILD]`)**.

**Все 15 уроков MVP построены (15/15). Дальше (`plans/2026-06-18-full-a0a1-scope.md`):**
- 🔴 **ПРИОРИТЕТ №1 — живой A0-тест Б** (`plans/2026-06-20-validation-criteria.md`): **ВСЁ ЕЩЁ НЕ ПРОВЕДЁН.**
  Совет (5/5, high) + грилл постановили это как главное; L11–L15 построены как осознанный override владельца.
  Продукт ни разу не встречал реального пользователя ЦА — единственный по-настоящему непроверенный риск.
  Решающее правило (PASS→деплой+дальше / FAIL→чинить онбординг/формат) и протокол — в плане валидации.
- ~~**tap-to-build**~~ — **ПОСТРОЕН** (2026-06-20, см. раздел «Построено» выше). Непостроенных модулей не осталось.
- **Мелкие лекс-дыры** (вне 15 уроков): числа 11–100 + плюрал (-s как тема) явно не введены; classroom-survival
  (алфавит/«произнести по буквам») — частично в разговорнике, отдельного скрипта нет. Вшивать только после теста.
- **✅ ДЕПЛОЙ ЖИВОЙ (2026-06-20):** `https://zoiruk.github.io/English-for-Seasonal-Workers/` отдаёт **200** и
  свежий билд (проверено: 15 `[BUILD]`, алфавит, числа 11–100, `sw.js`=esw-v33, движок `showBuild`). `origin` =
  `github.com/zoiruk/English-for-Seasonal-Workers` (публичный, default-ветка `master`; `Book A0-A1/` исключён;
  секретов нет). Pages-source = «GitHub Actions» включён владельцем; деплой авто на каждый push в `master`. (Первый
  репо `zoiruk/CC` НЕ использован — там другой июньский A1-курс на `main`, не тронут.) **Теперь возможен живой
  A0-тест Б с реального телефона по ссылке — приоритет №1.**
- **⚙️ Движок — quiz-shuffle:** до фикса все уроки имели правильный ответ на `c:0` (тык первой кнопки = 100%).
  Фикс в `app.js` (перемешивание опций + remap `c` при рендере) — урок реально проверяет. После фикса `c` в данных
  может быть любым; новые уроки писать с верным индексом, движок перемешает.

**2026-06-22 — L5: 3D-визуал предлогов места (по просьбе владельца, тест-режим).**
- Новое поле `grammar.visual_ru` (сырой inline-SVG, как `everyday.html` — **вне снежка/аудита**, англ. внутри не
  сканируется) + рендер в `app.js` `grammarBody` (карточка после intro, перед «Как сказать») + CSS `.g-visual svg`.
- Изометрические 3D-коробки + красный человечек показывают 6 предлогов: in/on/under(лёжа)/behind/near/next to.
  Вёрстка 2 колонки × 3 ряда (под телефон). Цвета текста — токены `--text`/`--text2`; коробка/человечек — литералы.
- **Нового английского НЕ добавлено:** 6 предлогов + транскрипции `[ин][он][андэ][бихайнд][ниэ][нэкст ту]` —
  байт-в-байт как в уже сверенной грамматике L5 (`forms.*.rule_ru`). `CACHE_VERSION` esw-v46→v47.
- Подписи увеличены по просьбе владельца (плотнее viewBox `56 12 364 550` + 3-строчные подписи, англ. шрифт 30):
  англ. подписи на телефоне ~26px (было ~13px), SVG 313×473.
- Проверено: `npm run audit` зелёный (audit/dedup/snowball 0 ошибок); рендер в реальном приложении (превью, mobile
  375px, без обрезки подписей, ошибок в консоли нет). **Слой-4 (`/lesson-naturalness-check L5`): оба агента упали на лимите
  аккаунта (сброс 13:10 Ташкент), формального source-cited прохода НЕТ.** Суть гейта выполнена косвенно (нового
  английского нет → нечего сверять); формальный проход с цитатами можно догнать после сброса лимита как paper trail.

**2026-06-22 — L11: 3D/перспективный визуал демонстративов (тот же паттерн `grammar.visual_ru`).**
- Матрица 2×2 «близко/далеко × один/много» в travel-контексте: дорога с перспективой + наблюдатель (red person) +
  автобус(ы) — this (1 близко) / that (1 далеко) / these (много близко) / those (много далеко). 3-строчные подписи,
  англ. шрифт 30 (~26px на телефоне). Цвета текста — `--text`/`--text2`; дорога/автобус/человечек — литералы.
- **Нового английского НЕ добавлено:** на картинке только this/that/these/those + транскрипции `[зис][зэт][зиз][зоуз]` —
  как в уже сверенной грамматике L11. is/are в картинку намеренно не вносил (фокус на близко/далеко×число; правило — в таблицах).
- `CACHE_VERSION` esw-v47→v48. Проверено: `npm run audit` зелёный; рендер в приложении (6 автобусов, 4 наблюдателя,
  12 подписей, без обрезки, SVG 313×462, ошибок в консоли нет). Слой-4 формально не гонял (как и L5 — нового англ. нет).

**2026-06-22 — L4: визуал часов (o'clock vs half past), паттерн `grammar.visual_ru`.**
- Два циферблата В СТОЛБИК (крупные, ~161px на телефоне): 7:00 = seven o'clock (минутная на 12, часовая на 7) и
  8:30 = half past eight (минутная на 6, часовая МЕЖДУ 8 и 9 → визуально снимает путаницу из `note_ru`: «half past
  eight» = половина девятого). 12 чисел на циферблате, легенда «короткая — часы, длинная (красная) — минуты».
- **Нового английского НЕ добавлено:** только seven o'clock / half past eight (есть в сверенной грамматике L4).
  Числа 11–100 не трогал — они отдельным html-справочником в табе «Фразы». `CACHE_VERSION` esw-v48→v49.
- ⚠️ Грабли вёрстки: 2 циферблата в РЯД распирали viewBox (широкие подписи) → мелко; решение — столбик (1 колонка).
  Проверка обрезки: `getBBox` даёт ЛОКАЛЬНЫЕ координаты (до `transform` группы) → ложные флаги; реальную обрезку
  мерить `getBoundingClientRect` против rect самого svg (числа в `<symbol>`/`<defs>` дают фиктивный bbox — игнорировать).
- Проверено: `npm run audit` зелёный; рендер в приложении (2 циферблата, без реальной обрезки, ошибок в консоли нет).

**2026-06-22 — Reading: «Книга про Ахмада» (graded reader, пруф 3 глав).** План — `plans/2026-06-22-reading-book-feature.md`.
- **Что построено:** новый режим Reading как **полка книг** (`window.BOOKS`, статика как `LESSONS`/`PHRASEBOOK`,
  **рантайм-генерации нет**). Сейчас одна книга «История Ахмада» (3 главы); структура готова под несколько книг —
  глава хранит `id == lesson id` (гейт разблока). Движок в `app.js`: карточка хаба `data-nav="library"` (📚 «Книги»),
  роуты `#library` / `#book/<bookId>` / `#book/<bookId>/<chId>` / `…/q` (старые `#reader*` → редирект на библиотеку);
  `renderLibrary` (список книг) → `renderReaderList(book)` (список глав, 🔒-гейт по `store.done[N]`) → `renderChapter(ch,book)`
  (крупный англ. текст, 🔊 на предложение, RU по тапу) + попап синего слова (`showGloss`: слово+транскр+RU+🔊) →
  `renderChapterQuiz(ch,book)` (RU-вопросы) → `finishChapter(ch,book)`. Стили — `style.css`
  (`.read-sent`/`.rs-en`/`.g-new`/`.gloss-pop`/`.lesson-card.locked`). `reader-audit.js` флэттит полку → главы.
- **v1 = пруф 3 глав L1–L2–L3** (приезд → семья/коллеги → вещи/инструменты), НЕ все 15. **Осознанный override владельца,
  как L11–L15:** приоритет №1 по STATE — живой A0-тест Б — НЕ проведён; сначала проверяем саму идею чтения на реальном
  пользователе, потом дописываем 4–15. Главы 4–15 откроются дозаписью в `READER`, гейт авто-подхватит.
- **Лексика:** снежок-связана. Тело главы N — только слова уроков 1..N + `activeWhitelist(N)` + `glossary[].en` (≤5 новых
  на главу, синие, **в SRS/счётчик/мастер-лексикон НЕ входят** — пассивное узнавание, реш. #10 плана). Стрик бампается
  за прочтение (учебное действие), `store.words` НЕ трогается. Глоссарий: ch1 big/new/happy/here; ch2 photo/here; ch3
  small/sharp/careful. IPA сверены (Wiktionary/Cambridge RP); транскрипция — вручную по конвенциям.
- **Аудит:** новый `scripts/reader-audit.js` добавлен в `npm run audit` (снежок глав + glossary ≤5/поля + quiz 4 опц./индекс +
  gloss-слово реально встречается в тексте). Зелёный.
- **🔴 Слой-4 (натуральность, source-cited) — ПРОВЕДЁН:** 2 адверсариальных ревьюера (коллокации/BrE + A0/safety) с цитатами
  + скептик-синтез. **Подтверждены и внесены 3 правки:** (1) `That is a hammer and a saw.`→ split `That is a hammer.`/`That is a saw.`
  (ед. демонстратив для двух предметов; цит. Cambridge demonstratives + s-v agreement); (2) `"Good job"`→`"You are a good worker"`
  (BrE = Well done, не в лексиконе; «good job» — амер. регистр; цит. Murphy/7ESL); (3) `He is a small baby.`→`He is a baby.`
  (контекст-коллокация; ревьюеры разошлись → взят однозначно-натуральный простой вариант, `small` убран из glossary ch2).
  Защищены как валидные: punnet/crate/trolley (BrE-ферма), `are a team`, `Be careful!` (A1-survival imperative).
- **Проверено в приложении** (превью, localStorage `done={1,2,3}`): хаб-карточка, список с 🔒-гейтом, чтение (14/13/17 предложений),
  RU-тоггл, синие слова (ch1 4 / ch2 2 / ch3 3) + попап, квиз (опции перемешиваются), `finishChapter` → `read[id]=true` + streak↑,
  `words`-счётчик НЕ изменился, гард прямого URL на закрытую главу → редирект на список. Ошибок в консоли нет.
- `index.html`: `reader.js` перед `app.js`. `sw.js`: `reader.js` в `PRECACHE`, `CACHE_VERSION` esw-v51→**esw-v52**.
- **Рефактор (владелец):** `window.READER` → `window.BOOKS` (полка книг; 1 книга «История Ахмада»); движок — `renderLibrary`
  (📚 «Книги») → книга → глава; роуты `#library`/`#book/<id>/<ch>/q`. Аудит и навигация консистентны, проверены.

**2026-06-23 — Детерминированные гейты против «неправильного английского» (3 скрипта в `npm run audit`).**
Цель: ловить классы ошибок, которые сборщик-агент может внести, а владелец (нулевой англ.) не заметит — **автоматически,
офлайн, на каждый коммит**, не полагаясь на внимательность агента/сеть. Дополняют (не заменяют) source-cited слой-4.
- `scripts/check-bre.js` — блок-лист американизмов (Good job→Well done, apartment→flat, color→colour…) во всех англ.
  полях уроков+ридера+разговорника. → автоматически валит «Good job».
- `scripts/check-transcr-safe.js` — грубая кириллица в любом `transcr` (класс `three→«сри»`). Литеральный матч (пробелы
  сохраняются) → «ху из» не ложно-срабатывает.
- `scripts/check-transcr-consistency.js` — транскрипция знакомого слова (≥5 букв из `words[]`/глоссария) едина во всех
  примерах/таблицах/диалогах/разговорнике (класс `tomorrow «тумороу»→«тэмороу»`). Слова вне `words[]` — на ревью.
- **Первый прогон сразу нашёл 2 реальных бага в готовом контенте** (не ложные): (1) L3 everyday `Be careful!` транскр.
  «кэ**й**фул» (звук /eɪ/) → «кэ**а**фул» (/eə/, по конвенции и как в ридере); (2) разговорник `Call an ambulance!`
  «**а**мбьюлэнс» → «**э**мбьюлэнс» (как в уроке-источнике + конвенция `/æ/→э`). Исправлены.
- Все 7 проверок зелёные. `CLAUDE.md` обновлён (гейты в «Процесс работы»/«Карта файлов»/«Грабли»). `CACHE_VERSION` esw-v52→**esw-v53**
  (правки `data.js`/`phrasebook.js`). Граница: блок-лист ловит ИЗВЕСТНЫЕ классы; мёртвые коллокации — по-прежнему source-cited слой-4.

**2026-06-23 — Ридер: дописываем книгу до 15 глав (партиями по 3, каждая через полный гейт + слой-4).**
- **Партия 1 — главы 4–6 готовы:** ч4 «Рабочий день» (L4 время/деньги; payslip без сумм-заработка — табу соблюдено),
  ч5 «Где что на ферме» (L5 предлоги/места), ч6 «Каждый день» (L6 Present Simple/распорядок). Глоссарий: ч4 fast/long,
  ч5 big/left/right, ч6 every/fast (IPA сверены Wiktionary RP; ни одно — НЕ слово из 455-лексикона).
- Слой-4 (2 ревьюера + синтез): A — чисто (payslip/half past/on the left/start work подтверждены BrE). B — 2 правки связности
  внесены: `Tom gives him a job`→`a crate` («give sb a job»=нанять, а Ахмад уже работает; цит. Collins/WordReference),
  `We pick berries`→`They pick berries` (скачок 1-го лица в 3-л. повествовании). 7 проверок зелёные, превью ок (ошибок нет).
- `CACHE_VERSION` esw-v53→**esw-v54**. Осталось: партии 7–9, 10–12, 13–15.
- **Партия 2 — главы 7–9 готовы:** ч7 «Что сейчас происходит» (L7 Present Continuous; глосс: slowly/quickly/busy),
  ч8 «Одежда и цвета» (L8 have/has + PPE/цвета; глосс: warm/safe), ч9 «Комната Ахмада» (L9 there is/there are + комната;
  глосс: small/dirty/nice). IPA сверены (RP). Слой-4 (2 ревьюера + скептик): 6 флагов, 6 отклонено — helmet/safe/do-you-have/
  I-have/dirty-room/good-worker все защищены как валидный BrE, соответствующий L8-грамматике (have≠have got) и нарративной логике.
  7 проверок зелёные. `CACHE_VERSION` esw-v54→**esw-v55**. Осталось: партии 10–12, 13–15.
- **Партия 3 — главы 10–12 готовы:** ч10 «В магазине» (L10 would like/how much + еда; глосс fresh/sweet/cheap; цена товара £2
  ОК, заработок не упоминается), ч11 «В город на автобусе» (L11 транспорт/which; глосс town/far), ч12 «Анкета в офисе»
  (L12 was/were + месяцы/форма; глосс first; контракт — только владение, без юр-утверждений). IPA сверены с конвенцией
  приложения (`/ɑː/→а`, `/ɜː/→ё`, `/iː/→и` — far→«фа» как car→«ка»).
  - **⚠️ Исправлен анти-паттерн happy/photo в партии 2:** ч7 глоссила `busy`(=L15-слово) и ч9 `nice`(=L15-слово) — обе
    замены на свободные: ч7 «Ahmad is busy»→«He is working in the rain» (глосс busy убран), ч9 «room is nice now»→«room is
    warm now» (глосс nice→warm). Впредь glossary-слова сверять против ВСЕХ уроков, не только 1..N.
  - Слой-4 (2 ревьюера + скептик): 2 правки внесены — `going to the town`/`in the town now`→`to town`/`in town now` (нулевой
    артикль для town-центра, как `at work`; «The town is far» как подлежащее оставлено — Longman/LDOCE), `fills the form`→
    `fills in the form` (BrE-коллокация; урок L11 уже использует «fill in the form», data.js:2128). Остальное (would like some,
    How much is the cheese/honey — uncountable, queue, was/were) подтверждено валидным BrE.
  - 7 проверок зелёные. `CACHE_VERSION` esw-v55→**esw-v56**. Осталось: партия 13–15.
- **Партия 4 — главы 13–15 готовы → 📖 КНИГА ПОЛНА (15/15).** ч13 «Прошлая смена» (L13 Past Simple; глосс then; стеммер
  снимает `-ed` → past-формы проходят снежок), ч14 «У врача» (L14 can/cannot + здоровье — зона повышенной ответственности;
  глосс kind/again), ч15 «Конец сезона» (L15 like/love/want + вечеринка-финал; глосс all).
  - **⚠️ Снежок-грабли:** `like/love/want` — whitelist-слова (не лексикон урока), а `reader-audit allowed()` НЕ стеммит whitelist
    → 3-е лицо `likes/loves/wants` падает. Решение: like/love/want — в прямую речь Ахмада 1-м лицом («I like this music!»).
    `goes`→`goe` (нерегулярный стем) → заменён на «He is at the doctor».
  - Слой-4 (2 ревьюера + скептик): 3 флага, 2 внесено, 1 отклонён. (1) ОТКЛОНЁН «did good work»→«did a good job»:
    «do good work» — валидный BrE, и сохраняет мотив good work/worker (ч3/9/12/13). (2) ВНЕСЕНО: RU-глосс «Cheers, mate!»
    «Будем»→«Будь здоров, дружище!» — «Будем» = только алко-тост, неверно для мусульманской ЦА + не учит частый смысл
    «спасибо/пока». (3) ВНЕСЕНО (safety): «Now Ahmad is better»→«The next day, Ahmad is better» — без разрыва во времени
    подразумевался мгновенный «вылечился за 1 приём → сразу на работу» (давление раннего возврата на уязвимого рабочего).
  - 7 проверок зелёные. `CACHE_VERSION` esw-v56→**esw-v57**. **Книга «История Ахмада» завершена: 15/15 глав, гейт разблока
    по `store.done[N]`.** ~~Открытый долг: словарная плитка `cheers` в `data.js:2587` несёт «будем»+🍻~~ → **ЗАКРЫТ
    (2026-06-23):** плитка `cheers` (L15, `data.js:2587`) переведена с «спасибо/будем (брит.)»+🍻 на «спасибо/пока (брит.)»+🙏 —
    совпадает с диалогом/everyday L15 и ридером (`reader.js:507` «Будь здоров, дружище!»); «будем» = алко-тост, неверен для
    мусульманской ЦА + 🍻 усиливал алко-прочтение. en/transcr/pn без изменений. 7 проверок зелёные. `CACHE_VERSION` esw-v57→**esw-v58**.

**2026-06-23 — 🚀 СТАРТ УРОВНЯ A2 (Elementary). План — `plans/2026-06-23-a2-scope.md` (grilled).**
Решение владельца: после A0–A1 (15/15) строить фермерское подмножество A2 (~8 уроков L16–L23). **Риск №1 (живой A0-тест Б
не проведён) сохраняется — A2 строится как осознанный override, как L11–L15/ридер.** Дизайн зафиксирован гриллом: грамматику A2
берём целиком, лексику — фермерский срез (туристический жир режем); правило ≥30 новых слов держим, грамм-блок паруем со свежим
фермерским полем (запас слов проверен — домен не исчерпан, ~250+ свободных); токенайзер расширен под сокращения; хаб — секции-уровни.
- **Фаза 0 — правка движка (`be17262`):** (1) `lib.js tokenize()` снимает `'ve/'re/'ll/'m/'d/n't` (+ `can't→can`, `won't→will`) →
  снежок матчит естественные A2-формы (have got/Present Perfect/couldn't); аддитивно, L1–L15 без регрессии (в их снежок-полях
  сокращений нет). (2) `app.js renderHub` — группировка по уровням (A0–A1 / A2), A2-карточки заперты до `store.done[15]`; сертификат
  A0-A1 развязан от `LESSONS.length` (scope = первые 15, иначе допиши A2 → молча «23/23»); инертно пока A2-уроков нет; CSS
  `.level-head`/`.level-lock`; UI-строки `level_a1/a2/a2_locked`. Проверено в превью: заперто+открыто рендерятся, 0 ошибок консоли.
  Стеммер `-er/-est` (comparatives) отложен в L17 (контекстный). `CACHE_VERSION` esw-v58→**esw-v59**.
- **L16 готов (going to + погода) — первый урок A2.** Грамматика: be going to (план + прогноз) +/−/?; диалог «погода и план смены»
  (manager↔worker); 30 новых слов погоды/сезонов (weather/forecast/sun/sunny/cloud(y)/wind(y)/rainy/storm/lightning/fog(gy)/mist/
  frost/ice/snow/warm/freeze/degree/breeze/season/spring/summer/autumn/winter/mud/soil/puddle/harvest). Снежок: going→стем go (L7),
  am/is/are/to/not — whitelist; **новых whitelist-слов не понадобилось** (going to едет на знакомых глаголах). `WHITELIST[16]` пуст.
  - **Слой 1 (audit):** 7/7 зелёные с первого прогона. **Слой 2:** verify-ipa — 1 флаг `cloudy` (нотация Wiktionary tie-bar, моя
    `/ˈklaʊdi/` стандартна, как sunny/windy), принят; verify-transcr — 0 флагов по L16. **Рендер:** превью — 5 табов, 30 карточек слов,
    квиз, 12 реплик диалога, 0 ошибок консоли.
  - **🔴 Слой 4 (source-cited натуральность, 2 адверсариальных ревьюера + инлайн-скептик):** 1 подтверждённая правка, внесена —
    `What is the weather today?`→`What is the weather **like** today?` (×2: диалог.1 + everyday; цит. Longman/British Council/Cambridge:
    `What's the weather like?` = стандартная BrE-коллокация, без `like` — калька). Защищены как корректные: прогноз через going-to от
    очевидности («Look at the clouds…» — British Council/Test-English A1), going to be + прилагательное, смешение план/прогноз в одном
    уроке (Test-English «plans and predictions» A1), дистракторы квиза (going to working / go to work / It going to rain), реплика про
    сарай (safety-нейтральна). Снежок `drive/wait` — в scope (зелёный). Скептик: `like` = ложный друг (учили «нравиться»), но курсы
    подают чанком с глоссом «какая погода» — флаг реальный, не ложный.
  - 7 проверок зелёные после фикса (`words[]` не менялись → verify-* не перегонял). `CACHE_VERSION` esw-v59→**esw-v60**.
- **L17 готов (comparatives & superlatives + описание) — 2-й урок A2.** Грамматика: короткое слово + `-er` + `than`,
  длинное → `more`/`the most`, превосходное `the …-est`, особые `good→better→best` / `bad→worse→worst`; 3-форменный
  каркас переиспользован под «📏 Короткое / 📐 Длинное / ⭐ Самый·особые» (ярлыки свободны, движок рендерит только
  `label_ru`). Диалог «сравниваем работу/ящики/погоду» (m↔w, переиспользует `going to` из L16). 30 новых прилагательных
  (slow/short/tall/strong/weak/wide/narrow/deep/high/low/quiet/loud/expensive/dangerous/easy/difficult/thin/young/clever/
  friendly/soft/thick/ripe/rough/tight/loose/bright/useful/bad/dark) — сверены против 455+30-словаря, дедупа, ридер-глоссария
  (исключены reader-gloss big/small/cheap/fast/long/safe/kind… → выбраны regular-сравнительные).
  - **🔧 Развилка стеммера `-er/-est` (план Фаза 0 #2) РЕШЕНА в пользу умного стеммирования:** `lib.js stem()` теперь
    снимает `-er/-est` для слов длиной >4 (guard защищает her/per/over/user). **Регрессионно-безопасно ПО ПОСТРОЕНИЮ:**
    стем только укорачивается, сырые формы остаются в `known`, поэтому ни один матч L1–L16 не теряется (доказано +
    подтверждено регрешн-прогоном `npm run audit` ДО и ПОСЛЕ). Регулярные формы (slower/taller/strongest/coldest)
    коллапсируют к базе автоматически; неправильные (где strip≠base) — в `WHITELIST[17]`: `than/more/most/best/worse/
    worst/heavier/heaviest/easier` (по аналогии с неправильными прошедшими L13). Удвоение/-y/silent-e (bigger/wider) —
    показаны в `note_ru` (вне снежка), практика на in-vocab `thin→thinner`.
  - **Слой 1 (audit):** 7/7 зелёные (регрессия L1–L16 чистая + L17). **Слой 2:** verify-ipa — 3 нотационных флага
    (weak `/wi(ː)k/`, dangerous Wiktionary-`/ʒ/`-артефакт, useful syllabic-`/l̩/`), мои RP верны; verify-transcr — 2 флага
    (young «янг», useful «юсфул») = конвенция /j/+гласная→я/ю (как music «мьюзик»), приняты. **Рендер (превью, A2 разблокирован
    через localStorage):** хаб-секция A2 + карточка L17, 3 кнопки форм + таблицы, 30 карточек слов, квиз — 0 ошибок консоли.
  - **Слой 3 (адверсариальные ревьюеры язык/IPA + педагогика/контракт):** внесено 5 правок — `thin` глосс «тонкий, худой»→
    «тонкий (предмет)» (достоинство: «худой» провоцировал сравнение тел коллег); эмодзи `loose` 🪢(узел=антоним)→〰️;
    в note удвоение `hot→hotter`→in-vocab `thin→thinner`; в `cultural_ru` добавлено различение «-er+than = ДВА / the…-est =
    из ВСЕХ»; +2 квиза на неправильные формы (good→better, bad→worse). **Отклонены ложные флаги:** `tomorrow`→«тэмороу»
    (это канон, фикс `ced0a86` был «тумороу»→«тэмороу»; совпадает с L16), «mislabel forms» (движок рендерит только `label_ru`),
    `clever/friendly` off-topic (план прямо включает «описание людей»).
  - **🔴 Слой 4 (source-cited натуральность, 2 ревьюера):** **SHIP, 0 подтверждённых правок.** Все сравнит./превосх.
    подтверждены против EF/Headway/Speakout/face2face Elementary + British Council/Cambridge (comparatives = A1–A2 grammar;
    `taller than`/`the strongest`/`more expensive`/`worse weather` — textbook BrE; `clever`/`ripe`/`loose` = BrE A1/A2 core,
    `clever` предпочтительнее AmE `smart`). Сравнение людей — только нейтрально/позитивно (taller/strongest), никого не
    унижает. 2 мягкие эмодзи-заметки (short 📏, weak 🥀) — в рамках политики «ближайшее честное», не блокеры.
  - `CACHE_VERSION` esw-v60→**esw-v61**. Осталось A2: L18–L23 (could+adverbs/have got/compounds/infinitive of purpose/take-get-have/Present Perfect).
- **L18 готов (could/couldn't + adverbs of manner + техника) — 3-й урок A2 (мердж 2 тонких блоков по плану).** Грамматика:
  `could` (прошлое умение + вежливая просьба `Could you…?`) +/− ; наречия «как» = прилагательное + `-ly` (careful→carefully,
  good→well особое, hard/fast без изменений). 3-форменный каркас: «✅ мог / ❌ не мог / 🛠️ Как?+-ly» (ярлыки свободны).
  Диалог «утро смены: не завёлся двигатель → провод → лезвие → механик» (m↔w). 30 новых слов техники/оборудования
  (trailer/engine/battery/fuel/diesel/tank/oil/switch/button/lever/blade/brake/gear/motor/crane/tyre/chain/cable/plug/wire/
  generator/power/spanner/nut/bolt/screw/clamp/fan/horn/pump) — сверены против словаря/дедупа/ридер-глоссария.
  - **Снежок:** наречия-manner + `could` = функц.-слова → `WHITELIST[18]=could/well/carefully/safely/easily/badly/quietly/loudly`
    (по плану: тонкие блоки в whitelist, 30 words[] от парного поля). `couldn't`→`could` через токенайзер Фазы 0 (естественный
    регистр). Reader-glossary `slowly/quickly/fast` НЕ в whitelist (только в note-прозе) — избегаем lesson/reader-оверлапа.
    Базовый предлог `with` никогда не вводился → заменён на known `near` (WL5), не ретрофитил.
  - **Слой 1 (audit):** 7/7 зелёные. **Слой 2:** verify-ipa — нотация/варианты (engine /e/, diesel/button syllabic, bolt
    `/bəʊlt/` мой RP верен vs Wiktionary `/bɒlt/`-артефакт); verify-transcr — `fuel` «фьюэл» (конвенция /fj/→фь, как «мьюзик»),
    `generator` выровнен «дженэрэйтэ»→«джэнэрэйтэ» (/e/→э, как engine→«энджин»). **Рендер:** 3 формы, 30 карточек, 0 ошибок консоли.
  - **Слой 3 (адверсариальные ревьюеры):** 6 правок — `tyre` глосс «шина, колесо»→«шина» (false-synonym), эмодзи `pump`
    🚰→💧, `The brake is bad`→`is broken` (естественнее для поломки), `+`good→well квиз (неправильное наречие не тестировалось),
    cultural-мост глоссов (`start the engine`=завести, `battery is low`=сел). Отклонено: «mislabel forms» (движок=label_ru),
    merge could+наречия (план), эмодзи switch/horn/blade (нет лучшей честной альтернативы).
  - **🔴 Слой 4 (source-cited, 2 ревьюера, фокус safety):** Reviewer B **SHIP** — **безопасность техники выверена против HSE**
    (gov.uk/hse.gov.uk «Using machines safely», «Safe maintenance»): все реплики = осведомлённость + «call the mechanic»
    (competent-person principle), НЕ инструкции-как-оперировать опасным. BrE подтверждён (spanner/tyre/diesel/lever vs AmE).
    `could`=A1–A2 (British Council). Reviewer A — FIX на коллокацию: `pump works easily`→`works well` (машина «works well»,
    Oxford/Cambridge), внесено; + смягчена реплика `The wire is loose. I can do it.`→`The wire is loose here.` (убран
    саморемонт электрики, HSE Safe Stop); в cultural добавлена брит. идиома `a flat battery` (осведомлённость; в снежок-полях
    оставлен прозрачный L17 `low`). Скептик-проход не нужен (фейл-флагов нет).
  - `CACHE_VERSION` esw-v61→**esw-v62**. Осталось A2: L19–L23 (have got/compounds/infinitive of purpose/take-get-have/Present Perfect).
