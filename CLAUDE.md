# CLAUDE.md — English for Seasonal Workers

Контракт проекта. Подгружается в каждую сессию. Правила здесь **переопределяют**
дефолтное поведение. Язык общения с владельцем — русский, код/комментарии/коммиты —
английский.

## Что это

Бесплатная веб-платформа изучения английского **с нуля (A0→A1)** для **сезонных
рабочих** из СНГ (Seasonal Worker Visa) в Великобритании. Цель — помочь выжить и
адаптироваться с первого дня на ферме.

- **Аудитория:** мигранты 20–45 лет, низкая цифровая грамотность, нулевой английский,
  бюджетный Android, нестабильный интернет, 15–30 мин вечером после смены.
- **Интерфейс: только русский (RU-only).** Мультиязычности (UZ/TJ/KG/KZ) сейчас НЕТ —
  не добавлять без явной просьбы. См. [decision](#чего-не-делать).
- **Контекст всего контента — ферма:** поля, пакхаусы, кэмпы, смены, безопасность,
  зарплата, документы. Слова/диалоги/примеры — только из жизни рабочего.

## Источник правды

1. **CEFR-PDF в `specs/`** (`CEFR A0-A1 … C1 …outline.pdf`) — канонический объём
   грамматики и лексики по уровням. Строить уроки строго в их рамках. MVP = A0-A1.
2. **`specs/*.md`** — продуктовые/тех/контент-спеки. ВАЖНО: они пришли из родственного
   проекта и описывают мультиязычную версию (`a1.html`, `translations.js`, `scratch/`,
   ключи `fe_*`, 5 языков). **Эти пути и 5-язычность к нам не относятся** — брать из них
   только принципы, адаптируя под RU-only и под реальную структуру (ниже).
3. **5 эталонных курсов = якорь натуральности и охвата** (НЕ забывать при любом контенте):
   New Headway Beginner (Oxford), Speakout Starter (Pearson), face2face Starter (Cambridge),
   English File Beginner (Oxford, `elt.oup.com/student/englishfile`) + CEFR A0-A1 PDF. Их
   scope-and-sequence сведён в `specs/coverage-matrix.md`; источники с URL + процедура —
   `specs/reference-sources.md`. **Правило: натуральность и охват любого нового/правимого текста
   сверять с этими источниками (и цитируемыми ELT/UK: British Council, Cambridge/Oxford, NHS/gov.uk),
   НЕ с суждением владельца** — владелец с нулевым английским не может быть приёмщиком натуральности
   (риск №1). Метод доказан: source-cited multi-agent сверка 246 строк текста → 2 правки (`0ab5e1f`).
   Гейт исполняемый: скилл `/lesson-naturalness-check <Ln>` = обязательный слой-4 проверки (см.
   «Процесс работы»). См. [[lesson-content-naturalness-anchor]], [[naturalness-check-is-mandatory-gate]].

## Текущее состояние (2026-06-20)

**Построено и проверено (аудит зелёный):**
- Движок: хаб + экран урока (табы Grammar/Words/Dialogue/Phrases/Quiz) + Web Speech +
  квиз-тренажёр + streak + PWA.
- **Уроки 1–9 «золотые»:** у всех есть `everyday` (функц. блок); в `quiz` типы `[LISTEN]`
  (аудирование: ▶ TTS → выбрать перевод, англ. текст скрыт) и `[GIST]` (чтение: RU-вопрос по диалогу).
  L1 — am/is/are + национальности (35 слов); L2 — притяжательные my/your/his/her; L3 — this/these + a/an;
  **L4 — числа/время/дни/деньги £; L5 — места + предлоги (Where is…?); L6 — Present Simple + do/does +
  наречия частоты + объектные me/him/us/them** (закрыта отложенная дыра); **L7 — Present Continuous +
  будущее-план (am/is/are + -ing; «I am starting tomorrow»); 30 новых -ing-глаголов (spray/sweep/read/
  climb/feed…), будущее и учится, и проверяется в quiz**; **L8 — have/has + do/does + одежда/PPE + цвета
  (30 слов); плейн `have/has` БЕЗ `have got`/сокращений (решение грилла — не трогать токенайзер; `have got`
  узнаваемой пометкой); quiz-фокус на согласовании he→has, does+have-база**; **L9 — there is/there are +
  some/any + комната/мебель/техника вагончика (room/bed/shower/heater/kettle/fridge…) + 8 состояний-прилагательных
  (broken/wet/cold/hot/heavy/empty/full/ready через is/are-предикатив); плейн `is not`/`are not`; закрыта дыра
  state-прилагательных, перенесённая из L8.** **L10 — how much/how many + would like (вежливое «хочу») +
  еда/напитки/покупки Tesco (bread/milk/rice/eggs/tin/biscuit/pasta/honey… 30 слов BrE); все ценовые вопросы —
  на неисчисляемых/ед.ч. (much↔many без скрытого price-исключения, чтобы не конфликтовать с ключом quiz);
  плейн `would not like` + «No, thank you»; диалог «магазин» worker↔cashier с сходящимся итогом-корзиной.**
  Снежок-whitelist по урокам — в `scripts/lib.js` (`WHITELIST[1..10]`, L10 добавил `many/would/like/of`).
- **Разговорник** (`app/phrasebook.js`, вход из хаба): 6 категорий / 62 фразы, адверсариально
  проверены (вкл. safety health/SOS/documents).
- **Режим повторения (SRS)** — флешкарты из пройденных слов (хаб 🔁). **Сертификат** 🏆
  (имя + статистика + print).
- Git ~72 коммита, чистая история; грамматика подтверждена 5 курсами (`coverage-matrix.md`).
  Всего 305 слов в 10 уроках (L1=35, L2–L10 ≈30).
- **Сверка IPA внешним якорем:** `npm run verify-ipa` тянет Received Pronunciation из Wiktionary
  для всех слов и сверяет с `pn` (267/305 совпали, вкл. L10; остаток — нотация, US-варианты снапшота,
  топонимы или нет в Wiktionary). Разрывает само-референтную LLM-петлю по фонетике (риск №1 совета).
  Вне `npm run audit` (сеть/медленно). L9 чист: флаги только нотация/варианты — `shower` /ˈʃaʊə/ (снапшот
  подтянул омограф «show-er»), `room` /ruːm/ (легитимный RP-вариант наряду с /rʊm/), слоговые `kettle/candle/broken`.
  **L10 чист: 0 расхождений с Wiktionary RP; 6 слов «не найдено» (cheese/butter/honey/tomato/carrot/banana) —
  частотные, BrE-IPA верны (tomato/banana с долгим /ɑː/, не US).**
- **Сверка transcr звуком:** `npm run verify-transcr` генерирует ожидаемую кириллицу из `pn` по
  конвенции курса и ловит несогласованности крутки (283/305, вкл. L10). Так найдены и исправлены `/w/`→«у»
  (work/worker/welcome) и `van`→«вэн». Вторая половина митигации риска №1 (крутка = фактический ввод A0).
  L9/L10 чисты: единственный новый флаг L10 — `onion` /ˈʌnjən/ «аньэн» (нотация /nj/ → «нь», осознанно);
  остальные 21 — старые исключения L1–L8 (топонимы, three→фри).
- **Сверка натуральности внешними источниками (`0ab5e1f`):** весь текст уроков + разговорника
  (246 строк: разговорник 62, диалоги 72, grammar-примеры 112) прогнан адверсариальным multi-agent
  workflow против авторитетных ELT/UK-источников (British Council, Cambridge/Oxford, NHS/gov.uk) +
  CEFR A0-A1-канона — каждый вердикт с цитатой-источником, второй проход отклоняет ложные флаги
  (британские `chemist`/`till`/`scales` защищены). Исправлено 2: `scale`→`scales` (BrE мн. ч. для
  весов, L3/L6) и `How much money is it?`→`It is five pounds.` (неидиоматичная коллокация, L4).
  Проверяет естественность ФРАЗ (коллокации/идиоматика/BrE) — ось, которую verify-ipa/verify-transcr
  (только фонетика `words[]`) не видят. Третий внешний якорь против ИИ-петли №1. Вне `npm run audit`
  (сеть/агенты); живой носитель остаётся гейтом перед публичным запуском (B1–B2).
  **Метод кодифицирован в скилл `/lesson-naturalness-check` (`.claude/skills/`, `4dd1f68`) = обязательный
  слой-4 гейт каждого урока; источники с URL — `specs/reference-sources.md`. L9 прогнан им: SHIP, 0 флагов
  (3 ревьюера, `e165775`). L10 прогнан слоями 3+4: SHIP после 5 правок — слой-3 поймал «how much + мн.ч.
  исчисляемых» (скрытое противоречие правилу much/many и ключу quiz) и «Good.» как реакцию на отказ; слой-4 —
  несходящийся итог корзины диалога (£10→£3, деньги = зона повышенной ответственности).**
- **Помощь «ещё проще» + YouTube-резерв** (движок `01e9ed1` + ретрофит L2–L7 `7920693`): свёрнутый
  блок в конце grammar-таба для тех, кто не понял правило. Опц. поля `grammar.simple_ru` (`formula` +
  1–2 мини-примера) и `grammar.ytQuery` (ручной RU-запрос). UX: тоггл (closed по умолчанию) → формула +
  примеры с TTS → мелкая последняя-резервная YouTube-ссылка (`target=_blank`, «нужен интернет») +
  инструкция возврата. **Есть на всех построенных уроках L1–L10.** Мини-примеры — дословно из уже
  сверенных таблиц/примеров урока (нет фонетического дрейфа). Аудит/снежок покрывают оба опц. поля.
  Спека: `plans/2026-06-19-grammar-help-youtube.md`.

**НЕ построено / дальше (`plans/2026-06-18-full-a0a1-scope.md` — актуальный чек-лист):**
- **Уроки 11–15** (тираж). Грамм/лекс-дыры вшиваются при их создании: чувства/welfare → L14, like+ing → L15,
  числа 11–100 + плюрал (-s как тема) → поздний урок. (Цвета — L8; будущее-план — L7; state-прилагательные — L9;
  `be going to` опц., не делаем.)
- **tap-to-build** (продукция из плиток-слов) — единственный непостроенный модуль.
- **`simple_ru`/`ytQuery` для L11–L15** — добавлять вместе с каждым новым уроком (часть «золотой» схемы),
  формула + 2 снежок-чистых мини-примера (брать из таблиц урока) + RU-запрос; через адверсариальное ревью.
- **Следующий шаг: L11** (this/that/these/those + транспорт/поездка: bus/ticket/station/stop). ⚠️ Сверить домен
  L11 в `master-lexicon.md` ↔ `coverage-matrix.md` и НЕ переиспользовать занятые слова (dedup залочил bank/shop/market).
  Каждый урок = полная схема: grammar + everyday + `[LISTEN]`/`[GIST]` + quiz, по одному уроку за сессию.

## Процесс работы (как делать урок)

1. Контент урока — в `app/data.js`; whitelist служебных слов и имена — в `scripts/lib.js`.
2. **`npm run audit`** (audit/dedup/snowball) — обязан быть зелёным перед коммитом.
3. **4-слойная проверка (все обязательны перед коммитом урока):**
   (1) автотесты `npm run audit`; (2) чек-лист по этому файлу (фермерская лексика, эмодзи по
   смыслу, RU-only, реальные `expl`); (3) **адверсариальные агенты-ревьюеры** (язык/IPA +
   педагогика/контракт) — ловят фонетику, чего скрипты не видят; (4) **🔴 source-cited сверка
   натуральности против 5 эталонных источников** (`specs/reference-sources.md`) — метод `0ab5e1f`:
   каждый вердикт с цитатой-источником + второй проход против ложных флагов. Запуск:
   `/lesson-naturalness-check <Ln>`. **Это НЕ generic-ревью «на вкус агента» — это неИИ-якорь
   против риска №1; пропуск слоя (4) = урок НЕ готов.** Вне `npm run audit` (сеть/агенты).
4. **Коммит по именам файлов** после каждого урока; бампнуть `CACHE_VERSION` в `app/sw.js`.
5. Превью: **`npm run serve`** (Node-сервер с `no-store`) → `localhost:8000`.

## Грабли (на что уже наступали)

- **Зелёный аудит ≠ корректно, если whitelist неверен.** Был баг: `my/your` ошибочно в
  whitelist Урока 1 → снежок проходил, но педагогически утечка (притяжательные — тема L2).
  Всегда сверяй whitelist урока с его реальной грамматикой.
- **Кэш превью:** `python http.server` слал файлы без cache-заголовков → старое в браузере.
  Поэтому `npm run serve` = Node с `no-store`, а SW — `network-first`. После правок бампай
  `CACHE_VERSION`; в браузере Ctrl+Shift+R.
- **Скриншот превью таймаутит на `backdrop-filter`** (плавающая «Назад») — проверяй через
  `preview_eval`/computed-style, не скриншотами.
- **Эмодзи-обманки** ревьюеры ловят часто. Нет точного эмодзи у слова → бери ближайшее
  честное ИЛИ замени слово на фермерское с прямым эмодзи.
- **Кириллическая `transcr` — фактический ввод** (латиницу A0 ещё не читает), поэтому она может
  нечаянно складываться в грубое/неуместное русское слово. Был кейс: `three`→«сри» (мат) → заменено
  на «фри» (`1f1838d`). При θ/коротких крутках проверяй прочтение; сверяй `npm run verify-transcr`
  (звук) + `npm run verify-ipa` (IPA). Конвенции крутки: `/w/`→«у», `æ`→«э», топонимы по рус. написанию.
  ⚠️ Оба скрипта покрывают ТОЛЬКО список `words[]`, НЕ транскрипцию в примерах/таблицах/диалогах/
  `simple_ru` — дрейф на уровне фраз слепая зона автоматики, ловит лишь адверсариальное ревью
  (кейс: L7 `tomorrow` «тумороу»→«тэмороу», `ced0a86`). Транскрипцию во фразах бери дословно из `words[]`.
- **Словоформы в снежке:** токенайзер в `lib.js` снимает `'s` и стеммит (`picking→pick`).
  Имена — в `NAMES` (исключение). Блок `everyday` снежком **НЕ** проверяется (заучиваемые фразы).
  ⚠️ Токенайзер снимает ТОЛЬКО `'s` (`he's→he`), но НЕ `'ve`/`n't`/`'ll`/`'m`/`'d` → `I've got`,
  `haven't`, `I'll`, `can't` падают в снежке (неизвестные токены). Решение L8 (грилл): учить плейн/полные
  формы в снежок-полях, сокращённые — только в `everyday`/`cultural` (вне снежка); токенайзер не трогать
  без явного согласия владельца. Повторится на Present Perfect/will/can't (L14). [[contractions-break-snowball-tokenizer]]
  ⚠️ **Короткие слова (≤3 букв) НЕ стеммятся** (`stem` ранний `return w`), но их плюрал стеммится в кашу:
  `bed`→`bed`, а `beds`→`b` → плюрал короткого слова НЕ матчит снежок (кейс L9, поймано аудитом). В снежок-
  проверяемых полях избегай плюрала ≤3-буквенных слов: «There are some **chairs/blankets**», не «beds».
  В `rule_ru`/`cultural_ru`/`expl` снежок НЕ сканирует — там натуральный `beds` допустим.
- **Объектные me/him/us** на A0 без глаголов не ввести естественно → введены в **L6** вместе с
  Present Simple (НЕ на L3, хотя в `structure.md` они там).
- **Большие фан-ауты (18–30 агентов) в одном workflow могут выбить лимит сессии** →
  предпочитать меньший параллелизм / последовательные мелкие workflow (память [[workflow-fanout-session-limit]]).
- **Контент строится с опережением видимого контекста** (восстановления/сжатия): прежде чем
  заявлять «X не построено» — сверься с `git log`, `plans/2026-06-18-full-a0a1-scope.md` и `app/`.

## Карта файлов

- `app/` — `index.html`, `app.js` (движок: хаб/урок/квиз/streak + экраны review-SRS,
  сертификат), `data.js` (`window.LESSONS`), `phrasebook.js` (`window.PHRASEBOOK` — разговорник,
  6 категорий `{cat, icon, title_ru, phrases:[{en,transcr,ru}]}`, **вне аудита/снежка**), `style.css`,
  `sw.js` (PWA), `manifest.json`, `icon.svg`.
- `scripts/` — `audit.js`/`dedup.js`/`snowball.js` + `lib.js` (whitelist+NAMES+токенайзер+стеммер),
  `serve.js` (dev-сервер no-store), `verify-ipa.js` (`npm run verify-ipa` — внешний Wiktionary-RP якорь
  для `pn`; снапшот `ref-ipa-wiktionary.json`, отчёт `ipa-verify-report.md`; вне аудита),
  `verify-transcr.js` (`npm run verify-transcr` — сверка кириллической `transcr` с IPA-производным
  звуком по конвенции курса; отчёт `transcr-verify-report.md`; ловит несогласованности крутки).
- `specs/` — CEFR-PDF (источник правды), `coverage-matrix.md`, `reference-sources.md` (5 эталонных
  источников + URL + процедура source-cited сверки), `master-lexicon.md` (слова+IPA+whitelist),
  прочие `*.md` — спеки чужого проекта (только принципы).
- `.claude/skills/` — `lesson-naturalness-check/` (скилл: source-cited сверка урока против 5 источников,
  метод `0ab5e1f`; обязательный слой-4 гейт перед коммитом урока).
- `plans/` — `2026-06-18-full-a0a1-scope.md` (дорожная карта A0-A1); `2026-06-19-grammar-help-youtube.md`
  (спека «ещё проще» + YouTube-резерв); `2026-06-19-phrasebook-youglish-check.md` (ручной YouGlish-чеклист
  разговорника — прослушка владельцем, не закоммичен). `_legacy/` — старый курс (архив).

## Референс-проект (НЕ пивотить)

`C:\Projects\English for Seasonal Workers (UK Life & Work` — зрелая **мультиязычная** версия
того же продукта (монолит `a1.html`, уровни A1/A2/B1, PWA, аудит-скрипты в `scratch/`).
Вердикт совета: **не пивотить и не импортировать целиком** (чужая архитектура fe_*/translations +
непроверенный, местами битый контент). Использовать ТОЛЬКО как источник идей; русский текст —
как черновик, переписанный в нашу схему и прогнанный через аудит.

## Решения по сборке (зафиксировано 2026-06-16)

Владелец делает проект сначала для себя (осваивает английский, ~1 урок/день, полное
освоение урока перед переходом к следующему), затем — как стартап с продвижением через
TikTok. Планка качества высокая: каждый урок практичный, интерактивный, со звуком.

Старый общий курс «English from Zero» **переписываем под фермерский формат**. Принятый план:

- **Архитектура:** урок = единый объект `{grammar, words, dialogue, quiz}`; навигация
  Hub-and-Spoke (главная-хаб со списком 15 уроков → экран урока с табами
  Grammar/Words/Dialogue/Quiz). `app/` переписываем с нуля.
- **Данные:** один внешний `app/data.js` → `window.LESSONS = [ … ]` (без `fetch`, offline).
- **Старое в архиве:** `lessons/`, `vocabulary/`, `tracks/`, старые `README/PROGRESS/HOW-TO-STUDY`
  перенесены в `_legacy/` (не удалять — могут пригодиться как черновик грамматики).
- **Сначала лексикон, потом код (Фаза 0):** мастер-лексикон уроков 1–15 (en + IPA из словаря +
  ru + emoji) со сверкой по CEFR-PDF и стартовым whitelist снежка — одобряется владельцем ДО кода.
- **Сборка вертикальным срезом:** затем движок + эталонный Урок 1 по «золотому стандарту»,
  владелец проходит его как первый пользователь, затем тираж уроков 2–15 (по одному в день).
- **MVP-движок (готов):** хаб+прогресс, урок с 4 табами, Web Speech, квиз-тренажёр, streak, PWA.
- **Полный объём A0-A1 (решено 2026-06-18, maximum scope):** по сверке с 4 эталонными
  Starter-курсами (`specs/coverage-matrix.md`) проект расширяется до полноценной платформы.
  Сверх 15 уроков грамматики добавляем: функциональный слой «Everyday English» + classroom-
  survival (алфавит/«не понимаю»), разговорник выживания, аудирование (listen-choose), чтение
  (gist), продукцию (tap-to-build), режим повторения (SRS), сертификат. Дорожная карта и
  порядок сборки — `plans/2026-06-18-full-a0a1-scope.md`. Грамматика покрыта 1:1 с CEFR;
  главный пробел был — функциональный слой и перекос в recognition.
- **Git:** репозиторий инициализирован для обратимости (данные урока живут в JS).

## Архитектура (Zero-Budget, Offline-First)

Без сервера, без БД, без внешних зависимостей (никаких JS/CSS-фреймворков).

- **Стек:** HTML + CSS + vanilla JS. Хостинг — GitHub Pages.
- **Аудио:** Web Speech API, `lang='en-GB'`, `rate=0.88`; всегда `speechSynthesis.cancel()`
  перед новым воспроизведением. Нет интернета → остановить и показать сообщение.
- **Хранение:** localStorage (прогресс по уровню, счётчик слов, streak, сертификат).
  Персональных данных на сервере нет, аутентификации нет.
- **Иконки:** emoji (эмодзи строго по смыслу слова). Картинок-файлов нет.
- **Производительность:** < 3 c на 3G, данные урока inline, работает offline после
  первой загрузки.
- **Совместимость:** Chrome/Samsung Internet/Safari iOS; Firefox Android — частично (Speech API).

## Формат данных урока (RU-only)

Один урок = объект с `grammar / words / dialogue / quiz`. Сейчас только русские поля
(`title_ru`, `rule_ru`, `tr_ru`, `hint_ru`, `ru`), плюс англ. `en`, IPA `pn`, кириллическая
`transcr`. **Localization-ready:** суффикс `_ru` сохраняем намеренно — добавить языки в
будущем = дописать `_uz/_tj/_kg/_kz` рядом, без переименований. UI-строки держим
централизованно (`UI.ru = {…}` + хелпер `t(key)`), чтобы локализация была дописыванием,
а не переписыванием разметки. Сам массив уроков — в `app/data.js`.

- **grammar:** 3 формы (✅ positive / ❌ negative / ❓ question), у каждой — `table`
  (`subj/verb/example/transcr/tr_ru`); `intro` через ❌/✅; `cultural` (различие с
  русским); `note` (типичная ошибка ⚠️). Суммарно **≥10 примеров**. Списки слов/местоимений
  в правилах — CSS-grid (`grid-template-columns:1fr 1fr`), не сплошной строкой.
  Опц. помощь «ещё проще»: `simple_ru:{formula, examples:[{en,transcr,ru}]}` (≥1, снежок-чисты) +
  `ytQuery` (ручной RU-запрос) → свёрнутый блок в конце таба. Оба опц.; нет → блок не показывается.
- **words:** **≥30** уникальных объектов `{e, en, transcr, ru, pn}`. По одному слову на
  элемент (запрещено `en:'one, two, three'`).
- **dialogue:** **≥10** реплик, чередование спикеров `s`: `m`(manager)/`w`(worker)/
  `c`(cashier)/`d`(doctor). Длина реплики 5–15 слов.
- **quiz:** **≥10** заданий `{q, opts[4], c, expl, hint_ru}`. Инструкции — только тегами
  `[COMPLETE]`/`[TRANSLATE]`/`[NEGATIVE]`/`[CORRECT]`/`[QUESTION]` + навыковые `[LISTEN]`
  (аудио ▶ → выбрать перевод, англ. текст скрыт) и `[GIST]` (RU-вопрос на понимание диалога).
  Английских инструкций в тексте быть не должно. `expl` — обязательное объяснение.
  Аудит: 4 уникальные опции, индекс `c` в диапазоне, **answer-leak** (ответ не стоит в вопросе,
  кроме `[TRANSLATE]`/`[LISTEN]`), уникальность реплик диалога.
- **everyday (опционально):** функциональный блок «Фразы» — `{title_ru, phrases:[{en,transcr,ru}]}`
  (≥3), survival-чанки с аудио, **вне снежка**. Если есть — у урока появляется 5-й таб «Фразы».

## Золотой стандарт контента (педагогика — НЕ нарушать)

1. **Снежный ком:** в грамматике/диалогах/тестах урока N можно использовать ТОЛЬКО слова
   из словарей уроков 1…N **плюс стартовый whitelist служебных/грамматических слов**
   (`am/is/are`, `not`, `do`, `a/the`, `this`, `where/what`, местоимения…), вводимых в
   блоке grammar и не входящих в 30 тематических. Без этого whitelist снежок на Уроке 1
   неисполним (учить отрицание/вопрос только «фермерскими существительными» нельзя).
2. **Уникальность словаря:** одно и то же англ. слово появляется в `words` только в одном
   уроке за весь курс (без дублей).
3. **Транскрипция везде:** кириллическая `transcr` обязательна для каждого слова, примера,
   строки таблицы и реплики; в правилах англ. слова оборачивать `<span class="g-transcr">`.
4. **Нулевой уровень:** объяснения простым языком без лингвотерминов; базовые частицы/
   местоимения переводить явно (`I am (я)`).
5. **Только фермерская лексика:** запрещены «городские/офисные»/нерелевантные слова
   (printer, computer, washroom attendant, apple и т.п.). Один урок = одна тема.
6. **Полнота:** грамматика ≥10 примеров, words ≥30, dialogue ≥10, quiz ≥10. Меньше —
   урок не готов.

> Эти правила проверяются Node-скриптами в `scripts/`: `audit.js` (структура/полнота),
> `dedup.js` (уникальность словаря), `snowball.js` (снежный ком). Запуск — **`npm run audit`**.
> Прогонять перед каждым коммитом урока; пустой список ошибок → урок «золотой».

## Контент-валидация и риски (из совета 2026-06-16)

Главный риск проекта: владелец с нулевым английским — и автор контента (через агента), и
приёмщик; аудит-скрипты ловят структуру, но НЕ фонетику/естественность. Поэтому:

- **IPA — из авторитетного словаря** (Wiktionary/CMUdict), не выдумывать. Кириллическая
  `transcr` — производная от IPA, подаётся как **затухающая опора** (больше в ранних
  уроках, меньше дальше), а не вечный костыль: цель — научить читать латиницу (payslip’ы,
  таблички). Слово всегда показывать латиницей + 🔊; кириллица вторична.
- **Мастер-лексикон — артефакт Фазы 0** (см. план): все слова 15 уроков с IPA, сверенные с
  CEFR A0-A1 PDF, ДО кода. Это снимает разом снежок, дедуп, CEFR-объём и галлюцинации IPA.
- **Зона повышенной ответственности:** деньги/зарплата, права, NHS/медицина, безопасность —
  это «лексика выживания»; неверная фраза = реальный вред. Такие категории проверять строже,
  без юридических/медицинских утверждений; табу про визу/заработок сохраняется.
- **CEFR — это области лексики, не жёсткий потолок слов.** «Фермерская лексика» = фермерское
  подмножество CEFR-областей (jobs→picker/packer, food→…, objects→punnet/crate). Объём ≥450
  достижим, но проверить по PDF до тиража.
- **Web Speech офлайн-зависим:** нет голоса/сети → не молчать, а показать сообщение и текст.
- **localStorage хрупок** (чистка кэша/переустановка стирает прогресс) → позже экспорт/импорт.
- **Владелец ≠ репрезентативный пользователь** (грамотнее, мотивированнее аудитории): его
  прохождение проверяет педагогику, но не доступность для A0/низкой цифровой грамотности.

## Дизайн (Telegram Web 2026, light-only)

- **Только светлая тема.** Тёмной нет — не добавлять.
- Clean Light: фон `#f0f2f5`, карточки `#fff`, текст `#1a1a2e`, акцент-синий `#0075de`.
- Системный sans-serif (`-apple-system, 'Segoe UI', Roboto`), без Google Fonts-зависимостей.
- Контейнер `max-width:430px; margin:0 auto` (мобильный приоритет, 320–430px).
- Навигация Hub-and-Spoke: главная-хаб + плавающая frosted-glass кнопка «‹ Назад» (iOS-chevron,
  `position:fixed`). Из хаба, кроме уроков, ведут плитки-входы: Разговорник 📒, Повторение 🔁
  (SRS, виден когда есть слова к повтору), Сертификат 🏆 — у каждого экрана своя frosted «Назад».
  Внутри урока — табы Grammar/Words/Dialogue/(Phrases)/Quiz; общая кнопка
  «Далее ➔» ведёт к следующему табу. В шапке урока — пилюля «N/всего».
- Карточки `border-radius:12–16px`; чипы pill `99px`; прогресс-бар `transition:width .5s`;
  нажатие `scale(0.98)`. Словарь — сетка 2 колонки (моб.) / 3 (широкий).

## Доступность

- Шрифт ≥14px (контент), ≥11px (метки/транскрипция); line-height ≥1.4; контраст WCAG AA.
- Touch target ≥44×44px, отступ между кнопками ≥8px, видимый отклик на нажатие.
- Не полагаться только на цвет: ✓ зелёный + «Правильно!», ✗ красный + «Неверно».
- Без горизонтального скролла: таблицы переносятся, кнопки 🔊 всегда в зоне видимости.

## Git-безопасность

Данные уроков живут внутри JS-файла → откат через git может стереть невосстановимый
контент. Поэтому: частые коммиты после каждого готового урока; бэкап массива перед
опасными операциями; стадить файлы по имени, не `git add .`. «Откати» = неразрушающий
просмотр (`git checkout <commit>`/`stash`/`log`), не `reset --hard`.

## Чего не делать

- ❌ Не строить СЕЙЧАС переключатель языков и 5-язычные данные (UZ/TJ/KG/KZ). Но архитектуру
  держать localization-ready (`_ru`-суффиксы + централизованные UI-строки) — локализация в планах.
- ❌ Не добавлять тёмную тему.
- ❌ Не вводить внешние зависимости/фреймворки, сервер, БД, аутентификацию.
- ❌ Не использовать в уроке слова вне «снежного кома» и нефермерскую лексику.
- ❌ Не хардкодить пути из чужих спек (`a1.html`, `translations.js`, `scratch/`, `fe_*`)
  как наши — сверяйся с реальной структурой репозитория.
